RedirectMatch 404 /\.git

<FilesMatch "\.(sql|md)$">
    Require all denied
</FilesMatch>

# .env (databaseadgangskode m.m.) og andre punktum-filer må aldrig kunne
# hentes direkte, uanset hvor de ligger.
<FilesMatch "^\.">
    Require all denied
</FilesMatch>
